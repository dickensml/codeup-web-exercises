

    // console.log('Hello from external Javascript');
    // alert('Welcome to my website!')
    // var color = prompt('What is your favorite color?');
    // alert('Great! ' + color +  ' is mhy favorite color, too.');


    // var littleMerm = (3);
    // var broBear = (5);
    // var Hercules = (1);
    // var rentalPrice = (3);
    // var totalpaid = (littleMerm + broBear + Hercules) * rentalPrice;
    // alert(totalpaid);

    // var googHours = (6);
    // var amazHours = (4);
    // var FBhours = (10);
    // var Grate = (400);
    // var Arate = (380);
    // var FBrate = (350);
    // var totPay = ((googHours * Grate) + (amazHours * Arate) + (FBhours * FBrate));
    // alert(totPay);

    // var classNotFull = (true);
    // var sched = (true);
    // var canEnroll = (classNotFull && sched);
    // alert(canEnroll);

    var items = (false);
    var offerGood = (false);
    var premMem = (true);

    var applyOffer = ((items && offerGood) || premMem);
    alert(applyOffer);